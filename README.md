### Prerequisites:

 - Python
 - Pipenv

### To start backend server:

 - `pipenv shell` 
 - `cd backend`
 - `python manage.py runserver`

### To start frontend server

 - `cd frontend`
 - `yarn start`